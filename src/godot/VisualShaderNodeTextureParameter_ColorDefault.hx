/**
	Generated using Godot Bindings Generator for Haxe.
	https://github.com/SomeRanDev/Haxe-GodotBindingsGenerator
**/
package godot;
@:cppEnum @:generated_godot_api @:bindings_api_type("global_enum") #if gdscript @:native("VisualShaderNodeTextureParameter.ColorDefault") #end @:is_bitfield(false) extern enum VisualShaderNodeTextureParameter_ColorDefault {
	/**
		Defaults to fully opaque white color.
	**/
	COLOR_DEFAULT_WHITE();
	/**
		Defaults to fully opaque black color.
	**/
	COLOR_DEFAULT_BLACK();
	/**
		Defaults to fully transparent black color.
	**/
	COLOR_DEFAULT_TRANSPARENT();
	/**
		Represents the size of the [enum ColorDefault] enum.
	**/
	COLOR_DEFAULT_MAX();
}